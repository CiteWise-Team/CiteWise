{
  "name": "CiteWise Enhanced Metadata Extractor",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "citewise-metadata-extractor",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-1",
      "name": "Webhook",
      "webhookId": "citewise-metadata-extractor-v2",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        240,
        800
      ]
    },
    {
      "parameters": {
        "jsCode": "const body = $input.first().json.body ?? $input.first().json;\nconst rawText = String(body.text ?? \"\").slice(0, 8000).trim();\nconst filename = String(body.filename ?? \"\");\nconst documentId = body.documentId ?? null;\n\nif (!rawText) throw new Error(\"No text provided\");\n\nconst doiMatch = rawText.match(/\\b(?:doi|DOI)[:\\s\\/]+([^\\s,;)\\]]+)/);\nconst doi = doiMatch ? doiMatch[1].replace(/[.,;)\\]]+$/, \"\") : null;\n\nconst arxivMatch = rawText.match(/arXiv[:\\s]+([0-9]{4}\\.[0-9]+)/i);\nconst arxivId = arxivMatch ? arxivMatch[1] : null;\n\nreturn [{ json: { rawText, filename, documentId, doi, arxivId } }];"
      },
      "id": "parse-1",
      "name": "Parse Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        464,
        800
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": false,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 1
          },
          "conditions": [
            {
              "id": "cond1",
              "leftValue": "={{ $json.doi || $json.arxivId }}",
              "rightValue": "",
              "operator": {
                "type": "string",
                "operation": "notEmpty"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "crossref-check",
      "name": "Has DOI",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        688,
        800
      ]
    },
    {
      "parameters": {
        "url": "=https://api.crossref.org/works/{{ $json.doi || (\"10.48550/arXiv.\" + $json.arxivId) }}",
        "options": {
          "response": {
            "response": {
              "neverError": true,
              "responseFormat": "text"
            }
          }
        }
      },
      "id": "crossref-1",
      "name": "CrossRef Lookup",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        912,
        736
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first().json;\nlet raw = item.output ?? item.text ?? item.response ?? item.message?.content ?? item.content ?? \"\";\nraw = String(raw).replace(/^\\s*```(?:json)?\\s*/i, \"\").replace(/\\s*```\\s*$/i, \"\").trim();\nconst firstBrace = raw.indexOf(\"{\");\nconst lastBrace = raw.lastIndexOf(\"}\");\nif (firstBrace === -1 || lastBrace === -1) return [{ json: { title: null, authorDisplay: null, authors: [], year: null, journal: null, doi: null } }];\nlet parsed = {};\ntry { parsed = JSON.parse(raw.slice(firstBrace, lastBrace + 1)); } catch(e) {}\nreturn [{ json: {\n  title: parsed.title || null,\n  authorDisplay: parsed.authorDisplay || null,\n  authors: Array.isArray(parsed.authors) ? parsed.authors : (parsed.authorDisplay ? [parsed.authorDisplay] : []),\n  year: parsed.year || null,\n  journal: parsed.journal || null,\n  doi: parsed.doi || null\n} }];"
      },
      "id": "parse-ai-1",
      "name": "Parse AI Output",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1936,
        800
      ]
    },
    {
      "parameters": {
        "url": "=https://api.semanticscholar.org/graph/v1/paper/search",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "query",
              "value": "={{ $(\"Parse AI Output\").first().json.title || $(\"Parse Input\").first().json.filename }}"
            },
            {
              "name": "fields",
              "value": "title,authors,year,externalIds,journal,venue"
            },
            {
              "name": "limit",
              "value": "1"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "neverError": true,
              "responseFormat": "json"
            }
          }
        }
      },
      "id": "semsch-1",
      "name": "Semantic Scholar Lookup",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2160,
        800
      ]
    },
    {
      "parameters": {
        "jsCode": "// Enhanced Metadata Extractor\n// Pipeline: Parse Input -> Has DOI -> CrossRef Lookup (text format) -> Semantic Scholar -> AI -> Merge\n\nconst body = $input.first().json;\n\nlet crossRefRaw = null;\nlet semSchRaw = null;\nlet aiRaw = null;\n\ntry { crossRefRaw = $('CrossRef Lookup').first()?.json; } catch(e) {}\ntry { semSchRaw = $('Semantic Scholar Lookup').first()?.json; } catch(e) {}\ntry { aiRaw = $('Parse AI Output').first()?.json; } catch(e) {}\n\n// Parse CrossRef - now handles both parsed JSON and raw text responses\nfunction parseCrossRef(raw) {\n  try {\n    if (!raw) return null;\n    // CrossRef Lookup now returns text format - parse it manually for safety\n    let parsed = raw;\n    if (raw.body !== undefined) {\n      // text format: { body: \"...\", statusCode: 200 }\n      if (raw.statusCode && raw.statusCode !== 200) return null;\n      try { parsed = JSON.parse(raw.body); } catch(e) { return null; }\n    }\n    const msg = parsed?.message;\n    if (!msg) return null;\n    const authorArr = (msg.author || []).map(a => {\n      const given = a.given || '';\n      const family = a.family || '';\n      return family && given ? `${family}, ${given}` : (family || given);\n    }).filter(Boolean);\n    const yearParts = msg.published?.['date-parts']?.[0] || msg['published-print']?.['date-parts']?.[0] || [];\n    const year = yearParts[0] ? String(yearParts[0]) : null;\n    const title = Array.isArray(msg.title) ? msg.title[0] : msg.title || null;\n    const journal = Array.isArray(msg['container-title']) ? msg['container-title'][0] : msg['container-title'] || null;\n    const doi = msg.DOI || null;\n    const volume = msg.volume || null;\n    const issue = msg.issue || null;\n    const pages = msg.page || null;\n    const publisher = msg.publisher || null;\n    if (!title && !authorArr.length) return null;\n    return { title, authors: authorArr, authorDisplay: authorArr.join(', ') || null, year, journal, doi, volume, issue, pages, publisher, source: 'crossref' };\n  } catch(e) { return null; }\n}\n\n// Parse Semantic Scholar\nfunction parseSemanticScholar(raw) {\n  try {\n    const papers = raw?.data || [];\n    if (!papers.length) return null;\n    const paper = papers[0];\n    const authorArr = (paper.authors || []).map(a => a.name).filter(Boolean);\n    const year = paper.year ? String(paper.year) : null;\n    const title = paper.title || null;\n    const doi = paper.externalIds?.DOI || null;\n    const journal = paper.journal?.name || paper.venue || null;\n    if (!title && !authorArr.length) return null;\n    return { title, authors: authorArr, authorDisplay: authorArr.join(', ') || null, year, journal, doi, volume: null, issue: null, pages: null, publisher: null, source: 'semantic-scholar' };\n  } catch(e) { return null; }\n}\n\n// Parse AI output\nfunction parseAI(raw) {\n  if (!raw) return null;\n  const authorDisplay = raw.authorDisplay || null;\n  return {\n    title: raw.title || null,\n    authors: Array.isArray(raw.authors) ? raw.authors : (authorDisplay ? [authorDisplay] : []),\n    authorDisplay,\n    year: raw.year || null,\n    journal: raw.journal || null,\n    doi: raw.doi || null,\n    volume: null, issue: null, pages: null, publisher: null,\n    source: 'ai'\n  };\n}\n\nfunction bestStr(...vals) {\n  for (const v of vals) { if (v && String(v).trim()) return String(v).trim(); }\n  return null;\n}\nfunction bestArr(...vals) {\n  for (const v of vals) { if (Array.isArray(v) && v.length) return v; }\n  return [];\n}\n\nconst cr = parseCrossRef(crossRefRaw);\nconst ss = parseSemanticScholar(semSchRaw);\nconst ai = parseAI(aiRaw);\n\nconst title         = bestStr(cr?.title, ss?.title, ai?.title);\nconst authors       = bestArr(cr?.authors, ss?.authors, ai?.authors);\nconst authorDisplay = bestStr(cr?.authorDisplay, ss?.authorDisplay, ai?.authorDisplay) || (authors.length ? authors.join(', ') : null);\nconst year          = bestStr(cr?.year, ss?.year, ai?.year);\nconst journal       = bestStr(cr?.journal, ss?.journal, ai?.journal);\nconst doi           = bestStr(cr?.doi, ss?.doi, ai?.doi);\nconst volume        = bestStr(cr?.volume);\nconst issue         = bestStr(cr?.issue);\nconst pages         = bestStr(cr?.pages);\nconst publisher     = bestStr(cr?.publisher);\nconst source        = cr ? 'crossref' : (ss ? 'semantic-scholar' : (ai ? 'ai' : 'none'));\nconst metadataReliable = Boolean(title && year && authorDisplay);\n\nreturn [{\n  json: {\n    documentId: body.documentId,\n    title,\n    authorDisplay,\n    authors,\n    year,\n    journal,\n    doi,\n    volume,\n    issue,\n    pages,\n    publisher,\n    metadataReliable,\n    source,\n    _debug: { cr: !!cr, ss: !!ss, ai: !!ai }\n  }\n}];"
      },
      "id": "merge-1",
      "name": "Merge Best Metadata",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2384,
        800
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ $json }}",
        "options": {}
      },
      "id": "respond-1",
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        2608,
        800
      ]
    },
    {
      "parameters": {
        "promptType": "define",
        "text": "=Extract citation metadata from this academic paper text:\n\nFILENAME: {{ $(\"Parse Input\").first().json.filename }}\n\nTEXT (first 8000 chars):\n{{ $(\"Parse Input\").first().json.rawText }}\n\nReturn ONLY valid JSON with this exact schema, no markdown fences:\n{\n  \"title\": \"full paper title or null\",\n  \"authorDisplay\": \"Author One, Author Two or null\",\n  \"authors\": [\"Author One\", \"Author Two\"],\n  \"year\": \"4-digit year string or null\",\n  \"journal\": \"journal or conference name or null\",\n  \"doi\": \"DOI string or null\",\n  \"metadataReliable\": true\n}\n\nRules:\n- title: the paper's full title, NOT the filename\n- authorDisplay: comma-separated real human names only; null if not found\n- Look for author names near affiliation markers (superscripts like 1,2,*, ?)\n- Look in text for \"Author contributions:\", \"Correspondence:\", \"Contact:\" sections\n- year: publication/submission year; for arXiv IDs like 2207.xxxxx the year is 2022\n- metadataReliable: true only if title AND year AND authorDisplay are all non-null\n- Never invent data; return null for any field you cannot find with confidence",
        "options": {
          "systemMessage": "You are a precise citation metadata extractor for academic PDFs. Extract only what is explicitly present in the text. Return null for any field you cannot confidently identify. Do not invent or guess author names or years."
        }
      },
      "id": "ai-1_copy0",
      "name": "AI Metadata Extractor",
      "type": "@n8n/n8n-nodes-langchain.agent",
      "typeVersion": 3,
      "position": [
        1584,
        -304
      ]
    },
    {
      "parameters": {
        "modelName": "gemini-2.5-flash",
        "projectId": "citewise-n8n-prod",
        "options": {}
      },
      "id": "gemini-1_copy0",
      "name": "Google Vertex AI Model (Gemini 2.5 Flash)",
      "type": "@n8n/n8n-nodes-langchain.lmChatGoogleVertex",
      "typeVersion": 1,
      "position": [
        1656,
        -72
      ],
      "credentials": {
        "googleApi": {
          "id": "weWyIYZsZQfNONDm",
          "name": "Google Vertex AI Account (CiteWise)"
        }
      }
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Parse Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse Input": {
      "main": [
        [
          {
            "node": "Has DOI",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Has DOI": {
      "main": [
        [
          {
            "node": "CrossRef Lookup",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "AI Metadata Extractor",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "CrossRef Lookup": {
      "main": [
        [
          {
            "node": "AI Metadata Extractor",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Google Vertex AI Model (Gemini 2.5 Flash)": {
      "ai_languageModel": [
        [
          {
            "node": "AI Metadata Extractor",
            "type": "ai_languageModel",
            "index": 0
          }
        ]
      ]
    },
    "AI Metadata Extractor": {
      "main": [
        [
          {
            "node": "Parse AI Output",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse AI Output": {
      "main": [
        [
          {
            "node": "Semantic Scholar Lookup",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Semantic Scholar Lookup": {
      "main": [
        [
          {
            "node": "Merge Best Metadata",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Merge Best Metadata": {
      "main": [
        [
          {
            "node": "Respond",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate"
  },
  "id": "SanSNUcZ0QwfDGeq"
}